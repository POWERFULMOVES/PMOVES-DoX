authorization {
    users: [
        { user: nats, password: pmoves }
    ]
}

websocket {
    port: 9222
    no_tls: true
    # Security model: Network isolation (Docker internal network)
    # TLS/auth handled by reverse proxy in production deployments
    same_origin: false
}


jetstream {
    store_dir: "/data/jetstream"
}
