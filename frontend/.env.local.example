# Base URL of the backend API (changed from 8000 to 8484 to avoid common port conflicts)
NEXT_PUBLIC_API_BASE=http://localhost:8484

# UI polling interval (ms) for background tasks/facts auto-refresh
NEXT_PUBLIC_POLL_INTERVAL_MS=3000
